{
  "name": "saint-augustin-website",
  "version": "1.0.0",
  "private": true,
  "description": "Saint Augustin Tourisme & Voyages - public site + backend + admin panel",
  "scripts": {
    "install": "npm install --prefix server",
    "start": "npm --prefix server start",
    "build": "npm install --prefix server"
  }
}
